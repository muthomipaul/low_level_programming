#include <stdlib.h>
#include <time.h>
#include <stdio.h>
#include "main.h"

/**
 * _strcat - Entry Point
 * @dest: character dest
 * @src: character src
 * Main function for concatenating two strings
 * Return: Always
 */
char *_strcat(char *dest, char *src)
{
	strcat(dest, src);

	return (dest)
}

