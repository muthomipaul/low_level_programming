#include <stdlib.h>
#include <time.h>
#include <stdio.h>
#include "main.h"

/**
 * _strlen - Entry Point
 * @s: character s
 * _strlen function for printing length of a string.
 * Return: Always
 */
int _strlen(char *s)
{
	return (strlen(s));
}

